let numArray=[1,2,3,4,5,6]
numArray.push(7) //to add new element to the last undex
console.log(numArray)


numArray.pop() // to dlt element from the last index
console.log(numArray)


numArray.unshift(0) //to add an element to the first index
console.log(numArray)

numArray.shift(0)    // to remove the first element from the first index
console.log(numArray) 
